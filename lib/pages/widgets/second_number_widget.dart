import 'package:flutter/material.dart';

class SecondNumber extends StatelessWidget {
  const SecondNumber({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Second Number');
  }
}
