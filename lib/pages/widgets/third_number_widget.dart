
import 'package:flutter/material.dart';

class ThirdNumber extends StatelessWidget {
  const ThirdNumber({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Third Number');
  }
}
