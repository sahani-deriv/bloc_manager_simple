import 'package:flutter/material.dart';

class FirstNumber extends StatelessWidget {
  const FirstNumber({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [
        Text('First Number'),
      ],
    );
  }
}
